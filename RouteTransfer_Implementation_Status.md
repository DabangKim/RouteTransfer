# RouteTransfer v0.1 구현·검증 결과

작성일: 2026-09-21. 개발 MVP를 구현했으며, 아래 검증 범위와 제한을 기준으로 시범 테스트할 수 있습니다.

## 구현한 기능

- 연결 관리·파일 전송·작업 기록·설정, 감사 기록 탭과 인증·전송 확인·취소 창.
- 연결 프로필 저장·수정·삭제, 서버 수·순서 변경, 서버별 포트·계정, 연결 테스트.
- Rust `russh` 기반 다중 SSH 경유, 최종 서버 SFTP. 파일 데이터는 경유 서버 디스크에 저장하지 않음.
- 최초 호스트 키 신뢰 확인, 저장된 키 변경 시 연결 차단. 서버별 비밀번호 입력과 선택적 OS 보안 저장소 연동.
- 로컬·원격 탐색, 다중 선택, 이름 필터·정렬·페이지 이동. 폴더 구조 및 빈 폴더 보존.
- 파일 하나씩 전송, 전체 및 파일별 진행량, 속도 표시, 작업·파일 취소.
- 충돌 기본 건너뛰기, 작업별 덮어쓰기, 배타적 임시 파일 생성과 최종 이름 반영. 기존 대상 선삭제 없음.
- 조사 후 원본·대상 변경 확인, 전송 후 크기 확인. 제품은 전 파일 체크섬 검사를 하지 않음.
- SQLite 기록, 파일별 시도 이력, 재시작 시 중단 복원, 완료 파일 제외 재시도.
- 최종 반영 중 종료는 `확인 필요`로 복원하여 자동 재전송하지 않음. 조사 중 종료는 `조사 미완료`로 구분.
- 기록 정리 미리보기와 실행 시 재검사. 미완료·확인 필요·임시 파일 참조가 있는 전송 이력은 삭제 대상에서 제외.
- macOS 앱 패키지, Windows NSIS 구성, GitHub Actions 수동 패키징 작업 정의.

## 실제 수행한 검증

| 검증 | 결과와 범위 |
|---|---|
| 프런트엔드 | TypeScript 검사 및 Vite 빌드 통과 |
| Rust | macOS 6개 및 Windows 7개 단위 검사 통과: 기존 대상 보존, 임시 파일 배타 생성, 새 스키마 거부, 중복 데이터 소유 차단, 재시작 상태 복구, 기존 대상 교체. Windows 예약 파일명 거부 포함 |
| 화면 유틸리티 | Windows/POSIX 경로 이동, 100 GiB 합산 크기 표시 검사 2개 통과. 실제 100 GiB 전송 검증은 아님 |
| 제품 SSH 엔진 | macOS 클라이언트 → 격리 Ubuntu 24.04/OpenSSH, 서버 1·2·4개 경로 통과 |
| 실제 전송 | 중첩·한글 경로, 빈 폴더, 양방향 전송 SHA-256 일치, 건너뛰기·덮어쓰기 |
| 중단 복구 | 32 MiB 파일 전송 중 프로세스를 종료한 뒤 재시작·수동 재연결·재시도 통과. 앞서 완료한 파일의 시도 횟수와 수정 시각 유지 |
| 인증 보호 | 잘못된 비밀번호 거부, 저장된 서버 키 변경 차단, 테스트 비밀번호가 SQLite/WAL에 없는지 확인 |
| 화면 연결 | 실제 제품 Core에 연결한 브라우저 검사: 프로필 저장·수정, 로컬 탐색·선택, 미연결 전송 차단, 감사·설정 화면, 1120px 최소 폭 검사 |
| macOS 패키지 | Apple Silicon/macOS 26.6.2에서 `.app` 빌드·실행, 네이티브 창과 실제 로컬 파일 탐색 확인 |
| npm 의존성 | 검사 시점 알려진 취약점 0개. Rust 의존성 보안 감사와 동일한 의미는 아님 |

검사 코드는 `tests/product_integration.py`, `tests/ui.mjs`, `tests/types.test.mjs` 및 Rust 단위 검사에 있습니다. 통합 검사는 제품이 사용하는 동일한 `Core`를 호출합니다. 기존 PoC 결과를 제품 테스트로 대체하지 않았습니다. UI 검사 브리지는 테스트 프로세스에서만 주입되며 제품에는 포함되지 않습니다.

## Windows 검증 패키지 생성 (2026-09-21)

[Windows x64 설치 파일 다운로드](https://github.com/DabangKim/RouteTransfer/actions/runs/35582150222/artifacts/10630932517) · [전체 빌드 결과](https://github.com/DabangKim/RouteTransfer/actions/runs/35582150222).

Windows GitHub runner에서 프런트엔드 빌드, 2개 화면 유틸리티 검사, 7개 Rust 단위 검사, x64 release 빌드와 NSIS 패키징이 성공했습니다. 설치 파일은 `RouteTransfer_0.1.0_x64-setup.exe`이고 WebView2 오프라인 설치 프로그램을 포함합니다. 패키지 ZIP은 약 231 MB입니다. 소스 커밋은 `55a1c32225757f7de11fe8942a16c90c2f167aed`입니다.

컴파일·단위 검사·패키징 성공과 실제 회사 PC 설치·SSH 접속 검증은 구분합니다. GUI 자동 실행 검사나 회사 서버 전송 검사를 Windows에서 수행한 것은 아닙니다. 서명되지 않은 내부 검증용입니다.

## 현재 제한과 설계 대비 구현 차이

- Windows 실행·설치, Intel Mac, 회사 폐쇄망의 실제 SSH 정책은 미검증입니다. 합산 100G 이상 전송도 사용자 지정 설계 가정이며 측정하지 않았습니다.
- 서버 수는 고정하지 않았고 4개까지 검증했습니다. 임의의 N개가 모든 환경에서 검증된 것은 아닙니다.
- 현재 한 번에 한 작업 묶음을 실행합니다. 실행 중 추가 조사·예약은 받지 않습니다. 한 묶음 안에서는 여러 파일을 순차 처리합니다.
- 목록은 폴더당 최대 100,000개, 화면은 200개씩 표시합니다. 폴더 열람 자체는 서버 목록을 메모리에 읽습니다. 작업·감사 화면은 최근 500건을 표시하며 그보다 오래된 DB 기록을 자동 삭제하지 않습니다.
- 초기 스키마 v1은 SQLite의 JSON 레코드와 관계 키를 사용합니다. 상세 설계의 정규화 테이블 전체, 전용 DB 작업 스레드, 마이그레이션 백업 체계는 이번 구현에 모두 적용하지 않았습니다. 새 버전 스키마를 구버전 앱이 여는 것은 거부합니다.
- 상태 갱신은 750ms 스냅샷 조회입니다. 상세 설계의 Channel·이벤트 순번·영속 요청 영수증은 후속입니다. 실행 잠금과 상태 검증으로 동시 전송을 차단합니다.
- 일반 파일과 폴더를 처리합니다. 심볼릭 링크·특수 파일·Windows reparse point는 제외합니다. 부모 경로에 링크가 있어도 차단하므로 macOS `/tmp` 대신 실제 경로 `/private/tmp` 등을 사용합니다.
- 서버 키 교체를 위한 전용 관리 화면은 없습니다. 키 변경은 차단한 채 지문을 표시합니다. 운영자가 키 변경 사유와 지문을 확인한 후 연결 프로필을 재등록할 수 있습니다.
- OS 보안 저장소 연동 코드는 구현했으나 실제 키체인 저장 허용·거부와 Windows Credential Manager 테스트는 남아 있습니다.
- 디스크 부족·권한 실패·포워딩 거부·SFTP 거부의 전용 장애 주입, 최종 rename 전후 각 지점의 프로세스 종료, 전원 장애 테스트는 아직 수행하지 않았습니다. 이 경우까지 검증 완료로 간주하지 않습니다.
- 변경 검사는 크기·수정 시각을 사용합니다. 동일한 크기·시각을 보존한 외부 변경이나 악의적인 동시 파일 교체를 완전히 탐지한다고 보장하지 않습니다.
- 서명·공증 및 GitHub Release 게시를 수행하지 않았습니다. Windows 패키징 워크플로는 실행하여 설치 파일 생성을 확인했습니다. 회사 PC 설치와 GUI 검증은 남아 있습니다.

다음 단계는 회사 서버에서 소량의 테스트 파일로 연결·양방향 전송·중단 후 재시도를 확인하고, 팀 배포 전 필요한 OS 및 장애 시나리오를 검증하는 것입니다.
